// A parameter that DOES reach a part file, so check_dependencies.py must report
// an edit class stronger than C1. Written because the checker had only a
// POSITIVE fixture (the change tree works) and no NEGATIVE one -- a check that
// can never report anything is indistinguishable from one that does not work,
// which is the "constant assertion" vacuity class in formal verification.
wall = 3;
height = 20;
