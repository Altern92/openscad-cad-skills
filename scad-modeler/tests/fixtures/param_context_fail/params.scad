// Direct reproduction of the real axle_d incident (INCIDENTS.md,
// 2026-08-18): a single shared diameter feeds two hardware-fit contexts;
// only one (diff_stub.scad) has a verifying assert.
module use_param(name, context, constraint) {}
axle_d = 6;
