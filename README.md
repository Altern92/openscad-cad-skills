# claude_skills

„Claude Code“ skills rinkinys (tai patys skill'as naudoja per `~/.claude/skills/` arba importuojant į projektą).

## Skills

| Skill | Aprašymas |
|-------|-----------|
| `openscad-cad` | OpenSCAD CAD projektavimas / modeliavimas |
| `scad-modeler` | OpenSCAD modeliavimo skill'as (platesnis, su scripts/templates) |

## Instaliacija

Kopijuok skill'ą į `~/.claude/skills/<skill-name>/` arba sukurk symlink:

```bash
ln -s <kelyje-į-šį-repo>/<skill-name> ~/.claude/skills/<skill-name>
```
